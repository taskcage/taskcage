# Scripts

This directory will contain reproducible development, installation and release scripts. Scripts must be non-interactive in CI, fail on errors, and never create cgroups outside the configured delegated subtree.
